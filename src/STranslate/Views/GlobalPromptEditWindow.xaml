<Window
    x:Class="STranslate.Views.GlobalPromptEditWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnvt="clr-namespace:STranslate.Converters"
    xmlns:control="clr-namespace:STranslate.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    Title="{DynamicResource GlobalPrompts}"
    Width="900"
    Height="650"
    MinWidth="800"
    MinHeight="600"
    ui:TitleBar.ExtendViewIntoTitleBar="True"
    ui:WindowHelper.SystemBackdropType="Mica"
    ui:WindowHelper.UseModernWindowStyle="True"
    ResizeMode="CanResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Window.InputBindings>
        <KeyBinding
            Key="Esc"
            Command="{Binding CloseCommand}"
            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
    </Window.InputBindings>

    <Grid Margin="24,40,24,24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  左侧：全局提示词列表  -->
        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  标题  -->
            <Border
                Grid.Row="0"
                Margin="0,0,0,16"
                Padding="12,8"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <TextBlock
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{DynamicResource GlobalPromptsList}" />
            </Border>

            <!--  操作按钮  -->
            <ikw:SimpleStackPanel
                Grid.Row="1"
                Margin="0,0,0,12"
                Orientation="Horizontal"
                Spacing="6">
                <Button
                    Padding="12,6"
                    Command="{Binding AddGlobalPromptCommand}"
                    Content="{DynamicResource Add}"
                    FontSize="{DynamicResource STControlFontSize12}"
                    Style="{StaticResource AccentButtonStyle}" />
                <Button
                    Padding="12,6"
                    Command="{Binding CloneGlobalPromptCommand}"
                    Content="{DynamicResource Copy}"
                    FontSize="{DynamicResource STControlFontSize12}" />
                <Button
                    Padding="12,6"
                    Command="{Binding DeleteGlobalPromptCommand}"
                    Content="{DynamicResource Delete}"
                    FontSize="{DynamicResource STControlFontSize12}" />
            </ikw:SimpleStackPanel>

            <!--  全局提示词列表  -->
            <Border
                Grid.Row="2"
                Background="{DynamicResource LayerFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <ListBox
                    Margin="4"
                    dd:DragDrop.IsDragSource="True"
                    dd:DragDrop.IsDropTarget="True"
                    Background="Transparent"
                    ItemsSource="{Binding GlobalPrompts}"
                    SelectedItem="{Binding SelectedPrompt}">
                    <ListBox.ItemContainerStyle>
                        <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="12,8" />
                            <Setter Property="Margin" Value="2" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="ui:ControlHelper.CornerRadius" Value="4" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <control:EditableTextBlock
                                    Grid.Column="0"
                                    Margin="5,0,0,0"
                                    FontSize="{DynamicResource STControlFontSize13}"
                                    Text="{Binding GlobalPrompt.Name}"
                                    ToolTipService.ToolTip="{Binding GlobalPrompt.Name}" />
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="8,0,0,0"
                                    FontSize="{DynamicResource STControlFontSize11}"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding ReferenceCount, StringFormat='({0})'}" />
                                <ui:ToggleSwitch
                                    Grid.Column="2"
                                    Margin="8,0,0,0"
                                    IsOn="{Binding IsEnabled}"
                                    OffContent=""
                                    OnContent="" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>

        <GridSplitter
            Grid.Row="0"
            Grid.Column="1"
            Width="8"
            HorizontalAlignment="Stretch"
            Background="Transparent" />

        <!--  右侧：编辑区域  -->
        <Grid Grid.Row="0" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  标题  -->
            <Border
                Grid.Row="0"
                Margin="0,0,0,16"
                Padding="12,8"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <TextBlock
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{DynamicResource GlobalPromptDetails}" />
            </Border>

            <!--  提示项管理  -->
            <ikw:SimpleStackPanel
                Grid.Row="1"
                Margin="0,0,0,12"
                Orientation="Horizontal"
                Spacing="10">
                <TextBlock
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{DynamicResource PromptItemManagement}" />
                <Button
                    Padding="12,6"
                    Command="{Binding AddPromptItemCommand}"
                    Content="{DynamicResource AddPromptItem}"
                    FontSize="{DynamicResource STControlFontSize12}"
                    Style="{StaticResource AccentButtonStyle}" />
                <Button
                    Padding="12,6"
                    Command="{Binding RemovePromptItemCommand}"
                    Content="{DynamicResource RemovePromptItem}"
                    FontSize="{DynamicResource STControlFontSize12}" />
            </ikw:SimpleStackPanel>

            <!--  提示项编辑区域  -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  提示项列表  -->
                <Border
                    Grid.Column="0"
                    Background="{DynamicResource LayerFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                    <ListBox
                        Margin="4"
                        dd:DragDrop.IsDragSource="True"
                        dd:DragDrop.IsDropTarget="True"
                        Background="Transparent"
                        ItemsSource="{Binding SelectedPrompt.GlobalPrompt.Items}"
                        SelectedItem="{Binding SelectedPromptItem}">
                        <ListBox.ItemContainerStyle>
                            <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="12,8" />
                                <Setter Property="Margin" Value="2" />
                                <Setter Property="ui:ControlHelper.CornerRadius" Value="4" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock
                                    FontSize="{DynamicResource STControlFontSize13}"
                                    FontWeight="Medium"
                                    Text="{Binding Role}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <GridSplitter
                    Grid.Column="1"
                    Width="8"
                    HorizontalAlignment="Stretch"
                    Background="Transparent" />

                <!--  提示项内容编辑  -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--  角色选择  -->
                    <Border
                        Grid.Row="0"
                        Margin="0,0,0,12"
                        Padding="12,10"
                        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <ikw:SimpleStackPanel Spacing="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center"
                                    FontWeight="Medium"
                                    Text="{DynamicResource Role}" />
                                <ComboBox
                                    Grid.Column="1"
                                    Padding="10,6"
                                    FontSize="{DynamicResource STControlFontSize13}"
                                    IsEnabled="{Binding SelectedPromptItem, Converter={cnvt:NullToBoolInverseConverter}}"
                                    SelectedItem="{Binding SelectedPromptItem.Role, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBoxItem Content="system" />
                                    <ComboBoxItem Content="user" />
                                    <ComboBoxItem Content="assistant" />
                                </ComboBox>
                            </Grid>

                            <!--  使用说明  -->
                            <Border
                                Padding="12,8"
                                Background="{DynamicResource SystemFillColorAttentionBackgroundBrush}"
                                BorderBrush="{DynamicResource SystemFillColorAttentionBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                                <ikw:SimpleStackPanel Spacing="4">
                                    <TextBlock
                                        FontSize="{DynamicResource STControlFontSize12}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource SystemFillColorAttentionBrush}"
                                        Text="{DynamicResource CustomPromptInstructionTitle}" />
                                    <TextBlock
                                        FontSize="{DynamicResource STControlFontSize11}"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        TextWrapping="Wrap">
                                        <Run Text="{DynamicResource CustomPromptInstructionDesc1}" />
                                        <LineBreak />
                                        <Run FontWeight="SemiBold" Text="{DynamicResource CustomPromptInstructionDesc2}" />
                                        <Run Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Text="$source" />
                                        <Run Text="{DynamicResource CustomPromptInstructionDesc3}" />
                                        <Run
                                            FontSize="{DynamicResource STControlFontSize10}"
                                            Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                            Text="{DynamicResource CustomPromptInstructionDesc4}" />
                                        <Run Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Text="$target" />
                                        <Run Text="{DynamicResource CustomPromptInstructionDesc5}" />
                                        <Run Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Text="$content" />
                                        <Run Text="{DynamicResource CustomPromptInstructionDesc6}" />
                                    </TextBlock>
                                </ikw:SimpleStackPanel>
                            </Border>
                        </ikw:SimpleStackPanel>
                    </Border>

                    <!--  内容编辑  -->
                    <Border
                        Grid.Row="1"
                        Background="{DynamicResource LayerFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <TextBox
                            Padding="12"
                            AcceptsReturn="True"
                            Background="Transparent"
                            BorderThickness="0"
                            FontSize="{DynamicResource STControlFontSize13}"
                            IsEnabled="{Binding SelectedPromptItem, Converter={cnvt:NullToBoolInverseConverter}}"
                            Text="{Binding SelectedPromptItem.Content, UpdateSourceTrigger=PropertyChanged}"
                            TextWrapping="Wrap"
                            VerticalScrollBarVisibility="Auto" />
                    </Border>
                </Grid>
            </Grid>
        </Grid>

        <!--  底部按钮  -->
        <ikw:SimpleStackPanel
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="12">
            <Button
                MinWidth="90"
                Padding="16,8"
                Command="{Binding SaveCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Content="{DynamicResource Save}"
                FontSize="{DynamicResource STControlFontSize13}"
                FontWeight="Medium"
                IsDefault="True"
                Style="{StaticResource AccentButtonStyle}" />
            <Button
                MinWidth="90"
                Padding="16,8"
                Command="{Binding CancelCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Content="{DynamicResource Cancel}"
                FontSize="{DynamicResource STControlFontSize13}"
                IsCancel="True" />
        </ikw:SimpleStackPanel>
    </Grid>
</Window>
